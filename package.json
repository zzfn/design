{
  "name": "design",
  "version": "0.0.0",
  "description": "个人博客组件库",
  "scripts": {
    "clean": "rm -rf node_modules packages/**/node_modules",
    "build:doc": "turbo run build",
    "lint": "turbo run lint",
    "build": "pnpm --filter @zzf/design run build",
    "publish:alpha": "pnpm publish --tag alpha --filter @zzf/design --no-git-checks",
    "publish": "pnpm publish --filter @zzf/design --no-git-checks"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zzfn/design.git"
  },
  "keywords": [
    "github",
    "ui"
  ],
  "author": "zzfn",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/zzfn/design/issues"
  },
  "homepage": "https://github.com/zzfn/design#readme",
  "devDependencies": {
    "zx": "^7.0.7"
  },
  "dependencies": {
    "turbo": "^1.3.1"
  }
}
