import { Modal,Button } from '../components'
import Playground from './Playground'

## 普通Modal

<Playground scope={{Modal,Button}} code={`
<Modal title='基本信息框' toggled={<Button>点击回复</Button>}>
This is the content of a basic modal.
More content...
</Modal>
`}/>
